# ==========================================================================
#  MINIMAL TEST .htaccess — tempotravellergoa.in
#
#  USE THIS ONLY FOR DIAGNOSIS.
#  If the full .htaccess gives 403, rename it to .htaccess-off, then upload
#  this file's contents as .htaccess and reload the site.
#
#    - Site loads OK  -> the 403 came from one of the removed rules.
#                        Add the numbered sections from the full file back
#                        ONE AT A TIME to find the guilty one.
#    - Still 403      -> the problem is NOT .htaccess. It's your host:
#                        file permissions, ownership, mod_security, or
#                        a missing index file. See the checklist in chat.
# ==========================================================================

DirectoryIndex index.html index.htm
Options -Indexes
AddDefaultCharset UTF-8

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>
